import React from 'react'

const Header = () => {
  return (
    <div>
      This project is to get more familiarity with React Context, Refs, React
      Query, and using JS APIs within React (specifically Intersection Observer)
    </div>
  )
}

export default Header
